# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.venv/
.env
*.env

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Agentarium run artifacts
runs/

# NOTE: uv.lock and backend/agentarium/static/ (the built web UI) are
# intentionally committed so the app installs deterministically and runs with
# no Node/npm step — see run.sh / README. Rebuild the UI with
# `cd frontend && npm run build` and commit the result.

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test / coverage
.pytest_cache/
.ruff_cache/
visual-artifacts/
htmlcov/
.coverage
